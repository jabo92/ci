name: Shell Commands

on: [push]

job:
  Run Shell Commands:
    run-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello World"
      - name: Multiline scripts
        run: |
          node -v
          npm -v