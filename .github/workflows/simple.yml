name: Shell Commands

on: [push]

jobs:
  Run-Shell-Commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello World"
      - name: Multiline scripts
        run: |
          node -v
          npm -v